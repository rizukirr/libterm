#include "internal.h"

/* TODO: lt_set_cell implementation — writes into back buffer */
int lt_set_cell(int x, int y, lt_uchar ch, lt_attr fg, lt_attr bg) {
    (void)x; (void)y; (void)ch; (void)fg; (void)bg;
    return LT_OK;
}
